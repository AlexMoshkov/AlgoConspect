---
cards-deck: Алгоритмы и структуры данных
---
### Амортизация и неасимпотическая оценка

#### Амортизированное время #card 
Операция работает за амортизированное время $O(f(n))$, если последовательность из $n$ операций в произвольном порядке на изначально пустой структуре работает за $O(n \cdot f(n))$
Операции $Op_1, \dots, Op_k$ работают за амортизированное время $O(f(n))$, если последовательность из $n$ операций $Op_1, \dots, Op_k$ в произвольном порядке на изначально пустой структуре работает за $O(n\cdot f(n))$^1654862401314
